{
  "name": "staking-system",
  "version": "1.0.0",
  "description": "TIPMAS MOL staking smart contract and web dashboard",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat run test/smoke.js",
    "deploy:tipmas": "hardhat run scripts/deploy.js --network tipmas",
    "frontend": "python3 -m http.server 5173 --directory frontend"
  },
  "keywords": [],
  "author": "TIPMAS",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.7",
    "hardhat": "^3.3.0"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "ethers": "^6.16.0"
  }
}
